import streamlit as st
import pandas as pd
import numpy as np

st.title("🏥 ระบบวิเคราะห์กลุ่มเสี่ยงแบบ Real-time")

# สร้าง Slider เลือกเกณฑ์ความดัน
threshold = st.slider("เลือกเกณฑ์ความดันที่ถือว่าเสี่ยง", 120, 180, 140)

# จำลองข้อมูล 1,000 คน
data = pd.DataFrame({
    'name': [f'Patient {i}' for i in range(1000)],
    'bps': np.random.randint(100, 190, 1000)
})

# กรองข้อมูลตาม Slider
filtered_data = data[data['bps'] >= threshold]

st.write(f"พบกลุ่มเสี่ยงจำนวน: {len(filtered_data)} คน")
st.bar_chart(filtered_data['bps']) # วาดกราฟแท่งอัตโนมัติ!